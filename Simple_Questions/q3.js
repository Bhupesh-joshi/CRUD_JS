// Write program, which reads 4 numbers a, b, c and p. Let fx)=ax-+bxte be a function. 
// The program outputs the value of flp). e.g input 4 3-1 2 output 4(2)+3(2)-1=21.

let a = 4, b = 3, c = -1;
let p = 2;

let value = a * p * p + b * p + c;
console.log(value);